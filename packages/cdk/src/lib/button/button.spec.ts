import { describe, expect, it } from 'vitest';
import { AegisButton } from './button';

describe('AegisButton (cdk brain)', () => {
  it('se define', () => {
    expect(AegisButton).toBeDefined();
  });
});
