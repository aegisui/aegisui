# Contrato: Fixture Bad Tokens

## Propósito

Fixture de regresión: a diferencia de `bad/` (que no tiene contrato en absoluto,
y por eso lo cubre `contract-exists`), este SÍ tiene contrato pero está
incompleto. Demuestra la dirección de fallo de `tokens-declared-in-contract`.

## Selector

`<aegis-fixture-bad-tokens>`

## Tokens que consume

- `--aegis-fixture-fg`

## Fuera de alcance

Todo. No es un componente de producto.
